%p#notice= notice
%h1 Listing Environments
%table
  %thead
    %tr
      %th Name
      %th Suite
      %th K8s master
      %th K8s username
      %th K8s password
      %th{:colspan => "3"}
  %tbody
    - @environments.each do |environment|
      %tr
        %td= environment.name
        %td= environment.suite
        %td= environment.k8s_master
        %td= environment.k8s_username
        %td= environment.k8s_password
        %td= link_to 'Show', environment
        %td= link_to 'Edit', edit_environment_path(environment)
        %td= link_to 'Destroy', environment, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Environment', new_environment_path
